{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": ".next",
        "installCommand": "npm install --include=dev typescript @types/react @types/node @types/react-dom",
        "buildCommand": "next build"
      }
    }
  ]
}
