{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "buildCommand": "npm install --include=dev && npm run build",
  "installCommand": "npm install --include=dev"
}
