{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": ".next"
      }
    }
  ],
  "buildCommand": "node vercel-build.js",
  "installCommand": "npm install --include=dev",
  "framework": "nextjs"
}
